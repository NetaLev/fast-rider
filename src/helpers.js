export const formatTime = (udate) => {
    return udate.substr(11, 5);
};

// export const getTicket = (selectedRide, updatedPin) => {
  
// };

// export const updatePIN = (updatedPin) => {
// };

// export const selectRide = (selectedRide) => {
// };